#importFrom(grDevices, check.options, dev.cur, dev.off, x11, png)
#importFrom(utils, browseURL)
#importFrom(lattice, lset, trellis.device)

import(graphics)
import(utils)

export(processRpadCommands,
       Rpad,
       startRpadServer,
       stopRpadServer,
       ROutputFormat,
       H,
       HtmlTree,
       HTML,
       BR,
       HTMLon,
       HTMLoff,
       HTMLh1,
       HTMLh2,
       HTMLh3,
       HTMLh4,
       HTMLh5,
       HTMLargs,
       HTMLtag,
       HTMLetag,
       HTMLradio,
       HTMLcheckbox,
       HTMLselect,
       HTMLinput,
       HTMLlink,
       HTMLimg,
       HTMLembed,
       graphoptions,
       newgraph,
       showgraph,
       RpadPlotName,
       RpadURL,
       RpadBaseURL,
       RpadBaseFile,
       RpadIsLocal
)

S3method(HTML, data.frame)
S3method(HTML, matrix)
S3method(HTML, default)
S3method(HtmlTree, HtmlTree)
S3method(HtmlTree, default)
S3method(print, HtmlTree)
